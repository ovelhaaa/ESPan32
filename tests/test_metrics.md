# Generated host metrics

This is only a format/documentation placeholder. `test_dsp` writes the
authoritative `build-host/test_metrics.md` on every run, and CI publishes that
generated file together with the deterministic `pan_*.wav` fixtures. Do not
manually copy result values into this source file.
